# zeek-systemd-udp

A Zeek container that includes systemd and the zeek-packet-source-udp plugin.
Use this if you want to use Zeek in environments where mirrored traffic
arrives encapsulated in GENEVE or VXLAN.
